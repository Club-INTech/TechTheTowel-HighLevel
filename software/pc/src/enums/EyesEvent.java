package enums;


/**
 * Cet enum décrit les différents états que les yeux peuvent représenter
 * @author discord, Aline
 */
public enum EyesEvent
{
    IDLE,
    BLOCKED,
    ENNEMY,
    SUCCESS,
    TEST,
    END;
}